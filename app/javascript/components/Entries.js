import React from "react";
import JournalForm from "./JournalForm"


const Entries = (props) = {title, entry, date, deleteEntries, updateEntries} = props

return(
  <div>
    <h1>{title}</h1>
    <p>{entry}</p>
    <p>Date:{date}</p>
    <button onClick=
  </div>
)